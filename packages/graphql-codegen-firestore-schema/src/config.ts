import {
  FirestoreCoreConfig,
  FirestoreCoreParsedConfig,
} from '@firebase-graphql/graphql-codegen-firestore-core'

export interface FirestoreSchemaPluginConfig extends FirestoreCoreConfig {}
export interface FirestoreSchemaPluginParsedConfig
  extends FirestoreCoreParsedConfig {}
